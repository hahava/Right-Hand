$(document).ready(function () {
    setNavActive("userInfo");
    setSubPageText("userToken");
    var addr = {"default": "/board/list", "type": 'userToken'};
    // set_page(data, page, addr);
    resizeFooterTag();

});